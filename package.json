{
  "name": "smart-clothesline-iot-system",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && npm run telegram:sync-webhook:auto",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit -p tsconfig.typecheck.json",
    "test:telegram": "node --test scripts/telegram-runtime-contract.test.mjs",
    "validate": "npm run lint && npm run typecheck && npm run test:telegram && npm run build",
    "telegram:sync-webhook": "node scripts/sync-telegram-webhook.mjs",
    "telegram:sync-webhook:auto": "node scripts/sync-telegram-webhook.mjs || node -e \"if (process.env.TELEGRAM_WEBHOOK_ENABLED === 'true' || process.env.TELEGRAM_WEBHOOK_SYNC_REQUIRED === 'true') process.exit(1); console.warn('[telegram:webhook] auto sync failed but build continues because webhook is disabled.');\""
  },
  "dependencies": {
    "autoprefixer": "^10.5.0",
    "firebase": "^12.12.1",
    "lucide-react": "^1.8.0",
    "mqtt": "^5.15.1",
    "next": "^14.2.35",
    "postcss": "^8.5.10",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^3.8.1",
    "tailwindcss": "^3.4.19",
    "zod": "^4.3.6",
    "zustand": "^5.0.13"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "eslint": "^8.57.1",
    "eslint-config-next": "^14.2.35",
    "typescript": "^5"
  }
}
